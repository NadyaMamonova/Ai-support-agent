[project]
name = "ai-ops-desk"
version = "0.1.0"
description = "Production-style FastAPI backend with AI agent"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic-settings",
    "sqlalchemy",
    "asyncpg",
    "alembic",
    "greenlet",
]